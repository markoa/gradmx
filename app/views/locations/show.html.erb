<%= page_h "#{@location.name}, #{@location.city.name}" %>

<%= @location.description %>

<% content_for :right_column do %>
  <%= link_to "New event", new_event_path(:location_id => @location) %>
<% end %>
